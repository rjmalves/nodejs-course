// There is a different for creating strings in JS

// Instead of using ' or ", we can use `.
// With this they become easier to format.

const name = "Max";
const age = 29;

console.log("My name is " + name + " and my age is " + age + " years old.")
console.log(`My name is ${name} and my age is ${age} years old.`)
